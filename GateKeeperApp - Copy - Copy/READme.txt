# 🛡️ Gatekeeper Vault

Gatekeeper is a secure, user-friendly Python script scanner and vault system designed to protect users from malicious code. Built with care and purpose, it empowers users to upload, scan, and safely store Python scripts in an isolated vault.

## 🚀 Features

- Upload & scan `.py` files for suspicious content
- Secure vault storage with folder isolation
- Emergency shutdown mechanism
- Vault Viewer with script preview
- Admin-only tools: search, copy, delete
- Dark-themed GUI with custom fonts and styling
- Easy-to-use `.exe` build for Windows

## 🔧 How to Use

1. Clone or download the repo
2. Run `gatekeeper_gui.py` with Python 3.10+
3. Upload a `.py` file to scan and store
4. Use Vault Viewer to browse, view, or manage scripts

## 🧪 Testing

Run the included test script:

```bash
python test_gatekeeper.py